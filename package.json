{
  "name": "andrii-kot-configs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "lint": "prettier -c . && eslint .",
    "fix": "prettier --write . && eslint . --fix"
  },
  "prettier": "@andrii-kot/prettier-config",
  "devDependencies": {
    "prettier": "^3.8.4",
    "eslint": "^9",
    "@andrii-kot/eslint-config": "workspace:*"
  }
}
